# Copy to .env in the project root and fill with your values
# Backend API URL (for Docker Compose: backend service is exposed on localhost:8000)
VITE_API_URL=http://localhost:8000

# Postgres Configuration (used in docker-compose.yml)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=paymock

# Optional: Supabase config (currently unused, kept for reference)
# VITE_SUPABASE_URL=https://your-supabase-url.supabase.co
# VITE_SUPABASE_ANON_KEY=your-anon-key
