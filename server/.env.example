# Copy this file to .env and fill in the values for Postgres + backend
# Postgres connection
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=paymock

# Full SQLAlchemy DATABASE_URL (optional — compose will set one automatically)
# Example: postgresql://postgres:postgres@postgres:5432/paymock
DATABASE_URL=postgresql://postgres:postgres@postgres:5432/paymock

# Backend port
PORT=8000
